<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>ToDos</title>
</head>

<body id="todos-body">
    <h1>Hello <%= name %>!</h1>

    <div id="buttons">
        <form action="/todos/add_todo" method="POST">
            <input type="text" name="title" id="title" placeholder="Add todo ( Title )">
        </form>

        <form action="/todos/remove_todo" method="POST">
            <input type="text" name="title" id="title" placeholder="Remove todo ( Title )">
        </form>

        <form action="/logout?_method=DELETE" method="POST">
            <button class="input" type="submit">Log Out</button>
        </form>
    </div>

    <% todos.forEach(todo => { %>
    <ul>
        <li><%= todo.title %></li>
    </ul>
    <% }) %>
</body>

</html>