<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ToDos</title>
</head>

<body>
    <h1>Hello <%= name %></h1>
    <p>Your user_id is <%= user_id %></p>

    <% todos.forEach(todo => { %>
    <ul>
        <li><%= todo.title %></li>
        <ul>
            <li><%= todo.description %></li>
        </ul>
    </ul>
    <% }) %>


    <button onclick="location.href = '/todos/add_todo'">Add ToDo</button>
    <button onclick="location.href = '/todos/remove_todo'">Remove ToDo</button>

    <form action="/logout?_method=DELETE" method="POST">
        <button type="submit">Log Out</button>
    </form>
</body>

</html>